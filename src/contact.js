var loadContactPage = {
    content: document.getElementById('content'),
    buildContactPage: function() {
        //create elements

        //add class names

        //add innertext to elements

        //append elements
    }
}